# Cloudflare Pages 原生响应头（静态资源层，不经 worker；网安报告 F-08 静态安全头补齐）
# worker 只处理 /api/* 的安全头（见 _worker.js applySecurityHeaders）；静态层在此统一加：
#   - HTML 的 HTTP 级 CSP（frame-ancestors 'none' 防点击劫持，meta CSP 无法表达此指令）
#   - HSTS / X-Content-Type-Options / Referrer-Policy 全站覆盖
# 注意：此文件与页面 meta CSP 同源同策略；CSP 内 script-src 'unsafe-inline' 为本站内联 onclick 架构所需
# Pages _headers 匹配规则：相对根路径 + 通配符；SPA 仅 index.html 一个 HTML 文档，CSP 只挂它

/*
  X-Content-Type-Options: nosniff
  Strict-Transport-Security: max-age=31536000; includeSubDomains
  Referrer-Policy: strict-origin-when-cross-origin

/index.html
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src https://fonts.gstatic.com; img-src 'self' data: blob:; connect-src 'self'; base-uri 'self'; form-action 'self'; frame-ancestors 'none'
  X-Frame-Options: DENY
  Permissions-Policy: camera=(), microphone=(), geolocation=()
