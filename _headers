# Cloudflare Pages 原生响应头（静态资源层，不经 worker；网安报告 F-08 静态安全头补齐）
# worker 只处理 /api/* 的安全头（见 _worker.js applySecurityHeaders）；静态层在此统一加：
#   - HTML 的 HTTP 级 CSP（frame-ancestors 'none' 防点击劫持，meta CSP 无法表达此指令）
#   - HSTS / X-Content-Type-Options / Referrer-Policy 全站覆盖
# 注意：此文件与页面 meta CSP 同源同策略（多策略取交集，浏览器强制）。V-4-1h h5a 收紧：
#   - script-src 去 'unsafe-inline'（v1 壳内联 onclick 已删，v2 零内联脚本）；
#   - style 拆分镜像 meta（style-src-elem 'self' 拦 <style> 注入 + style-src-attr 'unsafe-inline'
#     保 ui-modal cssText 数据通道——合并式 style-src 'self' 会让 style-src-attr 交集为空断 cssText）；
#   - 删 Google Fonts/font-src 例外（v2 零外部字体，v1 壳随删除消失）。
# Pages _headers 匹配规则：相对根路径 + 通配符；站点唯一 HTML 文档 = index.html（v2 壳）。
# v0.19.35（网安报告 F-08）：规则从 /index.html 改 /*——生产首页是 "/" 的 SPA 回退，
# 只挂 /index.html 时根路径与未知 SPA 路径裸奔；/* 统一覆盖根路径、SPA 回退与静态资源

/*
  X-Content-Type-Options: nosniff
  Strict-Transport-Security: max-age=31536000; includeSubDomains
  Referrer-Policy: strict-origin-when-cross-origin
  Content-Security-Policy: default-src 'self'; script-src 'self'; style-src-elem 'self'; style-src-attr 'unsafe-inline'; img-src 'self' data: blob:; connect-src 'self'; base-uri 'self'; form-action 'self'; frame-ancestors 'none'
  X-Frame-Options: DENY
  Permissions-Policy: camera=(), microphone=(), geolocation=()

# V-4-1e staging 冒烟抓出：v2 ESM 资产（dist/assets/*，esbuild 内容哈希名）此前无 immutable，
# 每次页面加载全量重下载 ~700KB。esbuild 名 = 内容寻址，同内容永不变名 → 可安全 immutable。
# worker 的 versionedBase 只覆盖 manifest 清单内文件（根/features 哈希 CSS）；/assets/* 走 ASSETS 直服，故在此声明。
/assets/*
  Cache-Control: public, max-age=31536000, immutable
