# 玻璃系统架构竞态分析（v0.19.7 现状）

> 生成于 2026-08-01 · 根因调查产物。逐组件列出当前 CSS 里所有"多源竞争 / 谁在管理覆盖"的屎堆。
> 目的：一眼看清每个玻璃组件上哪几坨屎在打架、谁胜出、是否意图。**铁律：竞争删一方。**

---

## 0. 结论摘要（TL;DR）

**根因（用户可见）**：`--g-liquid` 液态弯月整层画在**元素自身 inset box-shadow**，而玻璃体 `::before` 是 `z-index:-1`。CSS 绘制顺序里「元素自身背景/阴影」在 phase 1，「负 z 子元素」在 phase 2 且**画在其上** → 玻璃体把弯月整层盖住。全站玻璃只见毛玻璃、不见弯月。

- 像素证据（真实 glass.css，A/B 同卡对比，headless Chrome + PIL 采样）：
  - 左缘发丝线：A(当前)= 179 与内部无差 → **发丝线不存在**；B(弯月挪到 ::before)= 216 → **+37 清晰亮线**
  - 上缘亮带：B 比 A **+24**；整体内发光：B 全程比 A **+10**
- 连带受害者（同一根因，同一绘制顺序）：
  - `--g-hover` hover 白洗叠层（`inset 0 0 0 9999px`）→ 也被 ::before 盖住，按钮 hover 无白染
  - `.alert-error / .alert-success / .feedback-card--bug` 的左语义色条（`--g-lift: inset 3px 0 0`）→ 也被盖住

**架构结论**：玻璃体 `::before` 是"玻璃表面"。凡是**画在玻璃表面之上**的 inset 阴影（弯月、hover 白洗、语义色条）必须住在 `::before`；元素层只留外浮影（`--g-lift` 外阴影）、焦点环、组件自绘外阴影。当前代码把表面阴影放在元素层 = 结构性错位，不是数值问题。

---

## 1. 引擎层模型（设计意图 vs 现状）

glass.css 头部注释（13-17 行）声明的层模型：

| 层 | 设计意图 | 现状实际渲染 | 结论 |
|---|---|---|---|
| ① 全局舞台 `lg-bg` 光球+底板 | 页面级背景 | 正常（z-index:-2） | ✅ |
| ② 玻璃体 `::before`（z-index:-1） | 填充 `--g-fill` + 磨砂 `--g-frost` | 正常渲染 | ✅ |
| ③ 内容（元素自身 DOM） | 文字/子组件 | 正常 | ✅ |
| ④ 交互状态 + 液体弯月（元素 box-shadow） | `--g-liquid`/`--g-hover`/`--g-ring` | **被 ② 盖住，不可见** | ❌ 结构性错位 |

引擎三条主规则（`glass.css` 107-142）现状问题：
- 基础规则 `box-shadow: var(--g-lift), var(--g-liquid, none)` → 弯月画在元素层 = **被 ::before 盖**
- hover 规则 `box-shadow: ..., inset 0 0 0 9999px var(--g-hover)` → 白洗叠层也在元素层 = **被盖**
- focus 规则含 `var(--g-liquid)` → 无谓（反正不可见），焦点外环正常

---

## 2. 逐组件竞态清单（屎堆地图）

标记约定：**竞争点** = 同属性多源定义；**谁胜出** = 实际生效者；🟢意图 / 🔴屎 / 🟡 半屎。

### 2.1 卡族（list-card / post-card / about-card / notif-item / stat-card / auth-card / feedback-card / admin-row）

- 标记：`glass` ✅
- 背景：唯一主人 = `::before` 的 `--g-fill`（glass.css 162-166）。style.css 只设 padding/position/overflow/isolation。**无竞争** 🟢
- 阴影：引擎元素层 lift+liquid。弯月被盖（见 §0）。**根因屎** 🔴
- `stat-card::after`（style.css 898）左色条 z-index:1：与引擎 ::after 无冲突（stat-card 非 pressable），但与 ::before 的弯月同处左边区域，叠加渲染。🟡 低危
- overflow:hidden（.list-card style.css 855）：注意——**元素外阴影（lift）不被自身 overflow 裁剪，但 ::before 的外阴影会被裁剪** → 浮影只能住元素层。这是"阴影不能整体挪 ::before"的硬约束。⚠️ 修弯月时只能挪 inset 层，别挪外浮影。

### 2.2 按钮族（.btn + 变体 + tc-push-btn / chat-send / chat-back / btn-link-inline / btn-text-danger）

- 标记：`glass glass--pressable` ✅
- 背景：唯一主人 = ::before 的 `--g-fill`（各变体只设参数，style.css 148 只设 `background: none`）。**无竞争** 🟢
- 阴影：弯月 + hover 白洗被盖（§0）。**根因屎** 🔴
- `glass--pressable::after` 涟漪（z-index:0）：在 ::before 之上，正常。🟢
- overflow:hidden（.btn style.css 147）：同上浮影约束。⚠️

### 2.3 主页入口块 `.entry`

- 标记：`glass` ✅
- **文字色竞争**：style.css 271 `.entry { color: var(--ink) }`（特异性 0,1,0）直接写死 `color`；glass.css 334 设 `--g-fg: var(--paper)`（死变量，从不生效——引擎 `color: var(--g-fg)` 是 `:where()` 零特异性，被 style.css 覆盖）。**同一视觉意图两处声明，--g-fg 成死代码** 🟡 建议：删 glass.css 的 `--g-fg`，或改 style.css 用 `--g-fg`。
- **transform 竞争**：style.css 281/284 `.entry:hover/active`（translateX/scale）vs 引擎 `:where()` hover/active（translateY+scale）→ 特异性高者胜 = style.css，**正确**（组件胜出是设计）。🟢
- `::after` 竖条母题（style.css 276-282）：与玻璃 ::before 分占两个伪元素，无冲突。🟢
- **transition 竞争**：style.css 273（bg-color + transform）vs 引擎 113（transform + color）→ style.css 胜，引擎的 color 过渡失效。🟡 低危。

### 2.4 侧栏（sidebar-pill / sidebar-item / sidebar-user）

- `sidebar-pill`：标记 `glass` ✅。**背景竞争**：glass.css 204 设 `--g-fill: rgba(74,58,178,.66)`；style.css 568 已删黑底（注释"竞争删一方"）。当前唯一主人 = ::before。🟢（历史屎已清）
- `sidebar-item`：**非 glass**（手卷）。hover 白洗走 glass.css 267 `box-shadow: inset ...` 直接画元素（元素无 ::before，可见）。但它**绕过了引擎**，是第二套 hover 写法。🟡
- `sidebar-user`：标记 `glass` ✅，style.css 只设结构。🟢

### 2.5 头像 `.avatar`

- 标记：`glass` ✅（含 `.avatar--guest`、`.avatar--link`）
- 边框竞争：style.css 620 `.avatar--guest { border: 1.5px dashed var(--muted) }` vs glass.css 210 `border: 1.5px dashed rgba(110,110,118,.5)` → 同特异性，glass.css 后加载胜 → rgba 值生效。**同属性两处定义，靠加载序定生死** 🟡
- hover 环：style.css 1095 `.avatar-btn:hover .avatar--link { box-shadow: ring }` 覆盖引擎 lift → 自定义焦点环，意图。🟢 但 **ring 是元素层外阴影，正常**。
- 弯月：头像 border-radius 50%，`--g-liquid` 的发丝环（`inset 0 0 0 1px white`）被盖（§0）。**根因屎连带** 🔴

### 2.6 聊天气泡 `.chat-bubble`

- 标记：`glass` ✅（app-chat 模板）
- 背景：::before 单点（glass.css 176-179 三变体 --g-fill）。style-chat.css 173-176 只设结构。🟢
- 弯月走 `--g-liquid-sm`：被盖（§0）。🔴
- 注意：`.chat-bubble--media` 无 ::before 依赖冲突。🟢

### 2.7 浮层（modal / custom-select-panel / chat-plus-pop / profile-panel / profile-panel-head / modal-header）

- 标记：`glass glass--float`（浮层件）；头栏 `glass` ✅
- 背景：::before 单点。🟢
- **profile-panel 元素阴影竞争**：style.css 1122 `.profile-panel { box-shadow: -14px 0 44px }`（特异 0,1,0）覆盖引擎 `:where()` 的 lift+liquid → **profile-panel 元素层只有自绘左侧阴影**。弯月挪到 ::before 后该自绘阴影仍正常（外阴影不被 ::before 覆盖）。🟢（前提：外阴影在元素层，正确）
- 头栏 `--g-fill` 近不透明 + `--g-frost: blur(8px)`：意图。🟢

### 2.8 分段控件 / 页签（role-tabs / role-tab / score-mode-tabs / score-mode-tab）

- `role-tabs`：标记 `glass` ✅，::before = 玻璃体，::after = 滑动下划条（style.css 332-336）→ 分占伪元素，无冲突。🟢
- **`role-tab.active` 文字色双文件重复**（非冲突，已修）：style.css 343 与 glass.css 213 都是 `color: var(--ink)` **同值**——不是可见竞争（初稿误记为 #fff，实为同值死重复）。v0.19.7 已删 glass.css 213 的重复 color（背景/圆角保留），现单源在 style.css。🟢 已解决
- `role-tab.active` 背景：glass.css 213 硬编码 `background: rgba(74,58,178,.8)`（tab 非 glass，绕引擎直写）。**手卷第二套"实心选中"写法** 🟡
- **`score-mode-tabs` 双底双影**：标记 `glass glass--solid` ✅ 但 style-region.css 38 直写 `background: rgba(255,255,255,.1); border-radius; box-shadow: var(--glass-lift-sm)`（特异 0,1,0 胜 `:where()` 引擎）→ **元素层又画一底 + box-shadow 覆盖引擎 lift+liquid → 该组件弯月/浮影全被自己的手绘阴影顶掉**。且元素背景与 ::before 填充双层叠加 🔴
- `score-mode-tab`：标记 `glass glass--solid` ✅（模板 381-382 确认），active `--g-fill` 参数化走引擎。🟢

### 2.9 小件（tag 族 / checkbox-item / demand-id-tag / conv-signed-tag / profile-tag 等）

- 标记：`glass glass--solid` ✅
- **tag 语义背景 vs ::before 白填充双层**：`.tag-ok/.tag-danger/...`（glass.css 327-332）曾直写元素 `background`，而 ::before 有白填充+顶部径向高光 → **语义色被白填充压制**（元素 background 会被 ::before 盖住 = 层模型②的必然后果）。并发修复会话已把 tag 语义色改为 `--g-fill`（画在 ::before 上）→ 🟢 已解决
- checkbox-item：元素 `background: transparent` + checked `color:#fff`，填充走引擎 `--g-fill`（:has 切换）→ 干净。🟢
- 弯月：小件同样被盖（§0）。🔴

### 2.10 输入控件（form-input / form-textarea / custom-select-trigger / chat-input / filter-select）

- 标记：**非 glass**（替换元素不吃 ::before，glass.css 244-248 注释是硬约束）
- 自绘 `box-shadow: lift + inset 高光`（glass.css 247）：**唯一在元素层可见的 inset 高光**（因为没有 ::before 覆盖）。🟢 合理例外，注释已声明。

### 2.11 顶栏 / 侧栏 pane（navbar / client-sidebar / chats-list-pane）

- **navbar 背景三处竞争**：style.css 101 `background: var(--paper)` + 106 landing 渐变 + 981 移动端 `var(--paper)`；glass.css 278 `rgba(250,248,245,.52)` + 279 landing 渐变 + backdrop-filter。glass.css 后加载 + `:has()` 特异性高 → 桌面/移动 landing 渐变均胜。**同组件背景 5 处声明（2 文件），靠加载序+特异性收束** 🟡 半屎（能收口成 1 处）
- client-sidebar：glass.css 260-264 唯一主人（rgba 玻璃 + blur）。style.css 无竞争。🟢
- chats-list-pane（glass.css 269）：`background: rgba(255,255,255,.12)` 直写，非 ::before。🟢 低危（pane 非玻璃件）

### 2.12 `gk-pill`（grade-option 手卷玻璃）

- 标记：**非 glass**（`grade-option gk-pill`）
- **手卷第二套玻璃语言**：style-region.css 77-102 直写背景/hover/selected + ::before 竖条；且 `.grade-option.selected`（style.css 511 `background: var(--ink)`）vs `.gk-pill.selected`（style-region 100 `rgba(122,104,224,.78)`）→ 特异性 style-region 胜 → **selected pill 是紫不是墨**。**同一"选中 pill"语义两处背景、两套玻璃观感** 🔴
- 建议：要么给 gk-pill 挂 `.glass glass--solid` 走引擎（竖条迁 ::after），要么与 grade-option.selected 合并单点。

### 2.13 媒体查询 / 降级

- `@media (prefers-reduced-motion: reduce)`：`.lg-orb`/`.lg-mouseglow` 直接 `display:none`（glass.css 310-311）→ 光球整体消失，玻璃只剩静态。**用户开减弱动效 = 背景舞台消失**（设计决定，注释有据）。🟢 但若用户"什么都没看到"与系统开减弱动效有关，需告知。
- 移动端 860px：卡片 blur 降档、profile-card 加白底（glass.css 296-303）→ 意图。🟢

---

## 3. 屎堆分级清单（按严重度）

| # | 屎 | 组件 | 类型 | 处置 |
|---|---|---|---|---|
| S1 | 弯月/白洗/语义色条全被 ::before 盖住（元素 inset 阴影错层） | 全站玻璃 | 结构性错位（根因） | **本次修复：表面阴影挪 ::before** |
| S2 | `score-mode-tabs` 手绘背景+阴影顶掉引擎（双底双影） | 地区成绩页签 | 手卷第二套 + 引擎双轨 | 删 style-region.css 38 的 background/box-shadow，交还引擎 |
| S3 | ~~`role-tab.active` 文字色双文件竞争~~（实为同值 var(--ink)） | 角色页签 | 同值死重复 | ✅ 已删（v0.19.7） |
| S4 | `grade-option.selected` vs `gk-pill.selected` 两套选中语义 | 地区单选 pill | 双源语义 | 合并单点 / gk-pill 挂引擎 |
| S5 | navbar 背景 2 文件 5 处声明 | 顶栏 | 多源收束 | 收口到 glass.css 单点 |
| S6 | tag 语义背景被 ::before 白填充+高光洗淡 | 标签 | 双层渲染 | ✅ 已改走 `--g-fill`（并发修复会话落地） |
| S7 | `entry --g-fg: var(--paper)` 死变量 vs style.css 直写 color | 主页入口 | 死代码 | 删死变量（或改 style.css 用变量） |
| S8 | `.avatar--guest` border 双文件同特异性 | 头像 | 加载序定生死 | 合并单点 |
| S9 | `.glass--solid` 命名与行为不符（只关磨砂，不关高光/填充） | 小件 | 语义误导 | 重命名或补行为（低危） |
| S10 | sidebar-item / gk-pill / navbar 手卷 hover 写法 | 杂项 | 引擎旁路 | 逐步并入引擎（低危，可排队） |

---

## 4. 修复方案（状态：S1 核心已落地，本地 v0.19.6/v0.19.7 已提交未推）

> 状态更新 2026-08-01：根因 S1 的架构修复（liquid/surface/hover 迁 ::before）+ S3（role-tab 去重）已在本地提交 v0.19.6（c7c00c8）/ v0.19.7（7e4489c）落地，含 --g-surface 变量、语义条迁移、下缘暗弧加强（浅底可见，实测 -32）。S2（score-mode-tabs 双底双影）仍未处理。以下为完整方案与原则（供核对/收尾）。

### 本次修复（根因 S1）：表面阴影从元素层挪到 ::before

原则：**`overflow:hidden` 会裁剪 ::before 的外阴影 → 外浮影必须留元素层；inset 表面阴影（弯月/白洗/语义条）必须住 ::before**。

- `::before` 增 `box-shadow: var(--g-liquid, none), var(--g-surface, none)`
- 元素基础/ hover / focus 规则删 `var(--g-liquid)`
- hover 白洗挪到 `::hover::before`
- 新增 `--g-surface` 变量（默认 none），`.alert-error/.alert-success/.feedback-card--bug` 的 `inset 3px 0 0` 色条从 `--g-lift` 迁入
- 同步修 S2（删 score-mode-tabs 手绘双底双影）、S3（删 role-tab.active 文字色竞争方）

### 待做（建议排队，进 docs/backlog.md）

S4 gk-pill 并入引擎 · S5 navbar 收口 · S6 tag 语义色走参数 · S7 删 entry 死 --g-fg · S8 avatar 边框单点 · S9 .glass--solid 命名。

---

*验证方法：本地 headless Chrome 渲染真实 glass.css，A/B 同卡像素对比（亮带 +24、发丝线 +37）证明根因；修复后全组件截图核对弯月/白洗/语义色条恢复。*
