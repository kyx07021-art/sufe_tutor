{
  "name": "sufe-tutor-platform",
  "version": "1.0.0",
  "description": "上财家教信息共享平台 - 零佣金家教撮合服务",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx wrangler pages dev . --d1 DB --compatibility-date 2024-01-01",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name sufe-tutor",
    "test": "node --test \"test/*.test.js\"",
    "build": "node scripts/build.mjs",
    "deploy:dist": "npx wrangler pages deploy dist --project-name sufe-tutor",
    "test:arch": "node --test test/architecture-v2.archtest.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "jsdom": "^30.0.1",
    "playwright": "^1.62.1"
  }
}
