/**
 * Router stub (filled in M2)
 * -------------------------------------------------------
 * - Decision: Vue Router memory history (pure state routing, zero URL races) + last-page memory + role guard beforeEach.
 * - M2 (top bar + routing + auth state) implements this file; the M0 stage does not install vue-router, pages are rendered directly by App.vue for preview.
 * - Consumers: page registry (0/A/B/C + admin) and auth guards, see docs/adr/0003-new-frontend-auth-routing.md.
 */

// Stub: M2 creates createRouter({ history: createMemoryHistory(), routes, ... }) here and exports the router.
export const routerReady = false
