# M4 C2 会话模块 · 基元拆解清单（2026-08-22，复核修正 v2）

> 拆解 agent a14de8ae 产出 + M4-M6 复核 agent 修正（M4-06 拆三 + M4-23 判定独立 + 补验收边界列）。32→34 条基元，九阶段 A-I。**本工程最复杂模块**。

## 模块边界
做：双栏/选择卡/普通气泡/图片/文件气泡/特殊气泡视觉壳/提示文字/输入框组件/+号上拉栏/区内上边栏/结束会话/临时会话逻辑/发送动效/轮询。不做：起草合同、签约、合同内容（特殊气泡=视觉壳 data-cap，I-44..46 cap）。
接口：I-17~I-22（v2-ready）+ **I-23/I-24 临时会话（new，接口帽）** + 只读 I-15（合同灰掉结束按钮）。考古复用：气泡结构/文件映射/图片压缩/乐观 tempId+clientKey 幂等/轮询 data-mid 去重。

## 基元清单（34 条）
| 阶段 | # | 名称 | 复杂度 | 验收边界（变异/几何/负路径） |
|---|---|---|---|---|
| A 布局 | M4-01 | 双栏布局+路由注册 | 中 | 选择栏 20%/会话区 80% 视口内无溢出（G5） |
| A | M4-02 | 会话区顶部淡出遮罩 | 简单 | 遮罩不拦截滚动/点击（P22 穿透） |
| B 选择栏 | M4-03 | 选择卡（选中高亮/已结束灰化） | 中 | 选中切类/已结束灰化禁点（负路径） |
| B | M4-04 | 时间格式化映射 | 简单 | 映射单源 |
| B | M4-05 | **未读红点生命周期（四路径状态机）** | 复杂 | 发/收/读/退出四路径状态机变异（删一径红） |
| C 会话区 | M4-06a | 会话框架布局 | 中 | 消息滚动区/输入槽几何（G5） |
| C | M4-06b | 移动端切窗 | 中 | P22 负用例：移动端点会话聊天窗必须显示（几何断言） |
| C | M4-06c | 已结束只读门禁 | 中 | 变异：删门禁→已结束会话仍可发必红；输入框隐藏单点声明 |
| C | M4-07 | 消息加载渲染（最近N条游标） | 中 | 长会话尾部可见/无丢失窗口 |
| C | M4-08 | 普通气泡+品牌紫滤镜算法 | 中 | 滤镜纯函数直测 + **70% 最大宽几何断言（G5，规格 L412 我方/对方各 70%）** |
| C | M4-09 | 气泡发送时间映射 | 简单 | 单源 |
| D 附件 | M4-10 | 图片消息+大图查看 | 中 | 压缩尺寸上限/几何 |
| D | M4-11 | 文件气泡（小尾巴+线框+30/70） | 中 | 几何断言 |
| D | M4-12 | 文件类型 LOGO 适配器 | 简单 | 映射单源 |
| D | M4-13 | 文件名截断保扩展名 | 中 | 截断保留扩展名变异 |
| D | M4-14 | **附件暂存上传管线（压缩/进度/uploadId）** | 复杂 | F6 busy 变异/失败回滚 |
| E 特殊气泡 | M4-15 | 特殊气泡呼吸外框+内容槽（data-cap） | 中 | 视觉壳零 handler（data-cap 占位） |
| E | M4-16 | C2.6 提示文字（40%/移动80%） | 中 | 双 viewport 几何断言 |
| F 输入框 | M4-17 | 输入框组件布局（60px 胶囊/离底10px） | 中 | 几何（G5） |
| F | M4-18 | 纸飞机发送（Enter/Shift/IME） | 中 | IME 组合输入不误发变异 |
| F | M4-19 | 输入框自增高（向上/按钮沉底） | 中 | 自增高几何/按钮沉底 |
| F | M4-20 | **加号旋转变减号+上拉栏** | 复杂 | 旋转类切换/上拉栏几何 |
| F | M4-21 | +号上拉栏内容（图片/文件） | 简单 | 按钮接线（F4） |
| G 区内栏 | M4-22 | 区内上边栏（60px/用户名/更多） | 简单 | 几何 |
| G | M4-23 | 更多下拉栏渲染 | 中 | **只做下拉渲染**；「有合同灰掉」判定函数独立随 M4-25 接线 |
| G | M4-24 | 结束会话确认浮窗A（capToken） | 中 | 确认链路/capToken 二次认证 |
| G | M4-25 | **结束会话写路径+F7 同步+幂等+busy** | 复杂 | 变异：删同步/删幂等/删 busy 三红 + **灰掉门禁**（删 I-15 读取/删灰掉判定→有合同仍可点红；负路径：I-15 有合同→结束按钮 disabled） |
| H 临时会话 | M4-26 | **临时会话发起+发起方提示（I-23 帽）** | 复杂 | I-23 消费/发起方提示渲染 + 变异：删发起方提示→必红 + 负路径：未发消息临时会话仍驻留列表 |
| H | M4-27 | 限 1 条+输入框消失 | 中 | 与 M4-06c 共用输入框隐藏单点 + **变异：删输入框隐藏→必红（自身断言）** |
| H | M4-28 | 对方侧生成+提示+红点 | 中 | I-17 数据层契约驱动（前端零过滤）+ **负路径：对方侧未发消息前列表不显示** |
| H | M4-29 | 转正式+双方提示 | 中 | 状态翻转渲染/提示切换 + **变异：删状态翻转→必红 + 转正式后输入框恢复断言** |
| I 发送轮询 | M4-30 | **乐观发送管线（batch+幂等+回滚+F6）** | 复杂 | 变异：删幂等/删回滚红 |
| I | M4-31 | 发送气泡入场动效 | 简单 | 动效类切换 |
| I | M4-32 | **轮询增量（sinceId+data-mid 去重+预览 bump）** | 复杂 | 去重变异（重复消息不渲染）/预览 bump |

## 高风险基元（5-agent）：05/14/20/25/26/30/32（06c 状态门禁变异守护同靶点）

## 复核注记（M4 修正版复核 PASS，4 条件已落位）
1. **输入框隐藏单点契约钉死**：06c/27/29 三方共用单点 = `isChatInputVisible(state)`，由 **ended（已结束只读）+ temp-quota（限 1 条）双 flag 计算**，模块内唯一判定（写进 PROJECT.md）。
2. M4-25 灰掉门禁（I-15 有合同→结束按钮 disabled）验收已补。
3. M4-08 70% 最大宽几何断言已补。
4. M4-26/27/28/29 变异/负路径断言已补。
