# M5 C3 通知 + C4 更多 · 基元拆解清单（2026-08-22，复核修正 v2）

> 拆解 agent a9b159cc 产出 + M4-M6 复核 agent 修正（M5-09 拆三 + M5-00 级联消费 M0 + 补验收边界列）。17→19 条基元。

## 基元清单（19 条）
| 组 | # | 名称 | 复杂度 | 验收边界（变异/几何/负路径） |
|---|---|---|---|---|
| C3 入口 | M5-00 | 模块入口接线（openC3/openC4） | 中 | 只 wire 入口；附属树级联**消费 M0 共享底座**（模块内不再造） |
| C3 数据 | M5-01 | 通知数据层（I-26+缓存+unread 计数） | 简单 | 缓存读/计数单源 |
| C3 渲染 | M5-02 | 通知卡片 B1 | 简单 | 头像分支/省略号/红点/分割线不触边（几何） |
| C3 | M5-03 | 屏蔽系统通知复选按钮B | 中 | 按钮+I-28 持久化+列表过滤（同功能整体可滚；**变异：删持久化→刷新后按钮态丢失红 / 删过滤→系统通知仍显示红**） |
| C3 | M5-04 | 已读语义（单条+退出批量静默+失败回滚） | 复杂 | 变异：删回滚→失败后已读态残留必红 |
| C3 | M5-05 | 详情页+左出右入切换动效（浮窗内裁切） | 复杂 | 裁切几何/动效类切换 |
| C4 | M5-06 | 更多下拉栏（三选项+SVG+保留区联动） | 复杂 | 保留区防收起（变异删保留区→悬停收起红） |
| 设置 | M5-07 | 设置骨架（两栏+左切右滚+双向同步+无分割线） | 复杂 | 左切右滚同步/几何 |
| 设置 | M5-08 | 设置数据层（I-08/09 读写+F7） | 简单 | F7 同步 |
| 设置 | M5-09a | 用户名修改 | 中 | I-10 写路径+F7 变异（改后侧栏同步） |
| 设置 | M5-09b | 头像修改+居中裁切最大圆 | 中 | I-11+消费 M0 裁切；变异：删裁切→非圆必红 |
| 设置 | M5-09c | 联系方式绑定 | 中 | I-12 写路径+F7/占用 409 负路径 |
| 设置 | M5-10 | 外观区（主题/UI 缩放） | 简单 | 主题类切换 |
| 设置 | M5-11 | 设备管理（I-13） | 简单 | 列表零 token/逐端退登 |
| 设置 | M5-12 | 注销账户 | 复杂 | capToken+F7 登出清态（变异：删登出→陈旧 UI 红） |
| 关于 | M5-13 | 关于平台浮窗 | 简单 | 几何 |
| 反馈 | M5-14 | 反馈浮窗骨架（左栏 反馈/工单） | 简单 | 布局几何 |
| 反馈 | M5-15 | 反馈提交区（性质切换+匿名提交硬约束） | 复杂 | 匿名身份模型变异（泄露身份红） |
| 反馈 | M5-16 | 我的工单列表（匿名身份模型） | 中 | 匿名身份查询 |
| 收口 | M5-17 | 模块收口验证 | 简单 | 全链路+接口帽对账 |

## 需主会话签发（已签 §15）
I-26/27/28 形状、settings 字段集、**新增公开反馈提交+工单接口（匿名身份模型）**、C3/C4↔M2 触发契约。

## 复核注记（M5 修正版复核 PASS，7 条，开发时写入基元介绍）
1. **M5-00** 补被拦路径负用例：openC3 点击打开浮窗 + openC4 悬停呼出下拉，各一条（未登录/角色门禁下点击表现）。
2. **M5-03** 变异锚点已补（删持久化红/删过滤红）。
3. **M5-04** 补负路径：退出时仅对「被看到过」的通知批量标已读，未见通知保留红点（规格 :489 语义锁）。
4. **M5-07/M5-14** 复用关系：M5-14 消费 M5-07 或 M0 左栏切换底座（规格 :53 抽象底层元素，禁双份）；**M5-07 表述修正** =「左右栏之间有中间分割线、设置项之间无分割线」（规格 :559，原「无分割线」歧义）。
5. **M5-02** 复用 M4-03/M0 卡片 B1 基座（规格 :484 通知卡片样式与会话选择栏卡片差不多），防跨模块重复渲染。
6. **M5-15** 补性质切换（bug/建议/举报）各表单切换几何 + 切换后清态验收。
7. **M5-16** 补两路径断言：未登录 clientToken 查本端工单 / 登录用户 user id 优先。
M5-09b 与 M9-B2-6 双消费 M0 裁切工具（D2）：**M0 解冻程序预排该工具**，M0 未就绪时由首消费者建入 M0。
