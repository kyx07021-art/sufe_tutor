# S0 后端地基 · 基元拆解清单（2026-08-22）

> 拆解 agent a072d7a8 产出。26 条基元，7 群。依据 build-plan §2 + v2-reuse-boundary A 类 + interfaces §17（notify 核心归 S0）。

## 归属定界（读档定案）
notify 核心（notifyUser/建表/读/已读）归 S0；广播+批删+类型重登记归 S6-N2/N3。danger-ops 归 S0（A1 咽喉），各域消费方归各域。CSP 四源归 S0（部署管线），meta CSP 注入点归前端 M0 壳。reencrypt 核心归 S0，admin 入口归 S6-A9。部署管线全算 S0。audit-flow 内容域写断点归 S0，前缀适配放 S0 尾。otp/credential 归 S1。

## 开放问题定案（主会话，S0.md 内定案）
①**hash-assets 删除**（C 档）：Vite 原生内容哈希 + /assets/* immutable 已覆盖 manifest 改写职责；S0-24 落地删除 hash-assets.mjs，build.mjs 不再复制 manifest。
②**NOTIFY_TYPES 初版删减归 S6-N2**：S0-15 只搬注册表机制 + 保留 v2 全量类型（中间态可跑），删减不在此。
③**keepalive-worker 继续仓库外维护**（用户既有决策）：S0-25 只改打点域名/D1 绑定名。
④**CORS origin 用占位符**：域名未定前 S0-01/S0-23 验收以占位符一致为准。
⑤**S0 收口含最小端到端部署验证**：S0-24 之后跑 verify-staging-smoke 范式（health/登录/留档/通知全链路）。

## 基元清单（26 条）
| 群 | # | 名称 | 来源 | 复杂度 | 验收边界 |
|---|---|---|---|---|---|
| 0 shared | S0-01 | config.js 单源+站点改造 | 搬运+改造 | 复杂 | 数值/限额/限流/安全头迁入；站名/域名/版本替换；新站零新裸值 |
| 0 | S0-02 | codes.js 单源 | 搬运 | 中 | error(MSG) 恒非空 code；限额文案模板引用 config |
| 0 | S0-03 | enums.js 单源 | 搬运 | 中 | 状态/角色字面量统一；v2 键保留待 S3/S6 收敛 |
| 0 | S0-04 | region-data.js 单源 | 搬运+改造 | 中 | SUFE_REGIONS 唯一真源（契约 9）；上海 218 镇/街道+坐标 |
| A 安全 | S0-05 | secrets.js 密钥网关 | 搬运 | 简单 | 只读 env fail-closed/零明文回落 |
| A | S0-06 | crypto.js 加密咽喉 | 搬运 | 中 | AES round-trip；**fail-closed 变异**（删密钥→拒绝写红） |
| A | S0-07 | security.js 网安咽喉 | 搬运 | 中 | 限流双路径/三振/OTP per-IP 桶；CORS+安全头 |
| A | S0-08 | session.js 登录会话 | 搬运 | 中 | 多端会话/令牌 SHA-256 摘要落库 |
| A | S0-09 | danger-ops.js capToken | 搬运 | 中 | 落库失败返 ''（Z-1-F1）；原子 DELETE 消费 |
| A | S0-10 | startup.js Release Gate | 搬运 | 简单 | 缺密钥/管理员未轮换→503 not-ready |
| B 数据 | S0-11 | util.js + json.js 工具层 | 搬运 | 中 | ensureColumns 单点；parseBody 体积炸弹；UTC 单点；safeJsonArray |
| B | S0-12 | db.js 迁移编排骨架 | 搬运+改造 | 复杂 | initDb 幂等+版本门控+阶段编排+**域 schema 扩展点**（S1-S6 追加）；otp 调用点预留 S1-05 |
| B | S0-13 | version.js 版本戳 | 搬运+改造 | 中 | versionDomainOf 骨架+跨栈协议锁；路径映射随 S2-S5 更新 |
| C 咽喉 | S0-14 | log.js 留档咽喉 | 搬运 | 中 | detail 加密+敏感键剔除+dropped 计数（E1）；独立 LOG_DB |
| C | S0-15 | notify 建表+推送咽喉 | 搬运 | 中 | NOTIFY_TYPES 契约校验（D4 变异）；**全量类型中间态**（删减归 S6-N2） |
| C | S0-16 | notify 读/已读端点 | 搬运 | 中 | 列表形状+归属硬约束（跨用户 UPDATE 0 行变异） |
| C | S0-17 | text-audit 文本审核 | 搬运 | 中 | L1 门牌+HARMONIC 年份豁免；L2 **fail-closed 变异** |
| C | S0-18 | audit-flow 内容域写断点 | 改造 | 中 | 前缀与 S2-S5 实际路由 grep 对照零遗漏（S0 尾执行） |
| D infra | S0-19 | human-check.js captcha | 搬运 | 中 | 人机判定+防重放+I-07 路由；留档不翻转 verdict |
| D | S0-20 | reencrypt.js 密钥轮换核心 | 搬运+改造 | 复杂 | 游标分片+cursor 白名单；FIELD_TABLES 按新表适配（依赖 S2-S5） |
| E 管线 | S0-21 | router.js + app.js 声明式路由骨架 | 搬运 | 中 | 参数段直接匹配（Q-5-F1 不二次解码）+ 域 routes 拼接扩展点；零手写 if |
| E | S0-22 | _worker.js 编排层 | 改造 | 复杂 | SPA 回退冒充守卫/敏感路径 404/体积闸门/batch 禁 auth/check/Release Gate；initLedgerTable 绑定点预留 |
| E | S0-23 | _headers + CSP 四源姿态 | 搬运+改造 | 中 | 三源逐字一致+archtest 姿态锁；/assets/* immutable |
| E | S0-24 | 构建管线适配（build.mjs+deploy.sh+**hash-assets 删除**） | 改造 | 复杂 | **依赖前端 M0 Vite 产物**；最小可部署 dist+部署验证 |
| E | S0-25 | 运维/发布脚本适配 | 搬运+改造 | 中 | push-retry/announce（依赖 S6-N3）/wrangler/keepalive/d1 演练/分片 |
| F 契约 | S0-26 | 新站 archtest 契约骨架 | 新写 | 复杂 | 结构契约+CSP 四源锁+零裸值+变异负例 |

## 依赖与执行序（S0 内部 16 批 + 相对 S1-S6）
批 A（S0-01/05）→ B（02/03/04/11）→ C（06）→ D（07/08/17）→ E（09/10/13）→ F（14/19）→ G（15）→ H（12）→ I（16）→ J（21）→ K（22）→ **L（18 audit-flow 前缀，依赖 S2-S5 路由）**→ **M（20 reencrypt FIELD_TABLES，依赖 S2-S5 表）**→ N（23）→ O（24/25，依赖前端 M0 + S6-N3）→ P（26 收口）。
相对 S1-S6：S0 批 A-K 先行（地基）→ S1-S6 并行；L/M 与 S2-S5 并行后收口；S0-24 与前端 M0 并行。
