# S2 沟通与临时会话 · 基元拆解清单（2026-08-22）

> 拆解 agent a51bcb37 产出。15 条基元 = S2-T0（契约冻结）+ 6 搬运（B1-B6）+ 8 新写（T1-T8）。

## 临时会话状态机（验收核心锚点）
formal（temp_status=NULL）／temp_init（init，仅发起方列表可见、仅发起方可发、配额 1 条）／temp_sent（sent，首条落库事务 init→sent，接收方可见+红点+可回复，发起方被拦）／formalized（接收方回复事务 sent→NULL，temp_initiator 保留=wasTemp）。
数据模型：conversations 加 temp_status TEXT CHECK('init','sent') + temp_initiator_user_id（转正式保留）。

## 已定设计决策
①temp 结束=删除会话行（FK 级联）回归路人+零通知；formal 走 AI-1 close ②temp close 保留 capToken（与正式同口径）③init 态非发起方 404 防泄露 ④sent 态发起方超配 409 TEMP_QUOTA_EXCEEDED ⑤TEMP_SEND_QUOTA=1 进 config ⑥I-25 详情属 S1 ⑦temp 按双方角色定型。

## 基元清单（15 条）
| # | 名称 | 来源 | 要点 |
|---|---|---|---|
| S2-T0 | interfaces 签发 temp 契约 | 新写 | I-23/I-24 形状冻结（前端 M4 接帽） |
| S2-B1 | chat 三表搬运+kind 收敛迁移 | 搬运+清理 | kind 去 signing 两值/全表重建 carry |
| S2-B2 | 会话/消息读写搬运 | 搬运 | 游标/reopen/参与方 404 |
| S2-B3 | 消息批量发送搬运 | 搬运 | client_key 幂等/事务/batch |
| S2-B4 | 附件暂存搬运 | 搬运 | dataURL 黑名单/净化/配额 |
| S2-B5 | 关系/结束关系搬运 | 搬运 | my-relations/close 级联/winner |
| S2-B6 | 签约残留清理 | 清理 | bindable-demands+6 signing 函数删除（W18） |
| S2-T1 | temp 列+迁移 | 新写 | temp_status+temp_initiator/bump |
| S2-T2 | temp 常量单源 | 新写 | TEMP_STATUS/LIMITS/codes |
| S2-T3 | POST temp 创建/复用 | 新写 | I-23 元组查/formal reopen/404 |
| S2-T4 | 列表 temp 过滤+字段 | 改造 | init 仅发起方显/sent 接收方见 |
| S2-T5 | 详情 temp 字段+init 门禁 | 改造 | 404 不泄露/wasTemp 推导 |
| S2-T6 | **发送路径 temp 状态机** | 新写 | 规则 C 五 agent/单 batch 原子/双兜底 |
| S2-T7 | temp 结束回归路人 | 改造+新写 | 删行零通知/capToken |
| S2-T8 | temp 测试全量 | 新写 | 状态机全链路/负用例/变异 |
